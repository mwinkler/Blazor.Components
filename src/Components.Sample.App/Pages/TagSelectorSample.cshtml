@page "/TagSelector"

<TagSelector SelectableTags=@Tags SelectedTags=@Selected Filter=@((item,term) => item.Contains(term)) OnTagSelected=@(item => StateHasChanged()) OnTagRemoved=@(item => StateHasChanged())>
    <TagTemplate>@context</TagTemplate>
</TagSelector>

Selected: @Selected.Count

@functions {

    IEnumerable<string> Tags = new[] { "Item 1", "Item 2", "Item 3", "Item 4", "Item 5", "Item 6" };
    IList<string> Selected = new List<string>();

}
